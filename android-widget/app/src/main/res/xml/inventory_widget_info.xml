<?xml version="1.0" encoding="utf-8"?>
<!--
  updatePeriodMillis is a *minimum*, and Android clamps anything under
  30 minutes up to 30 minutes regardless of what's set here — that's
  an OS-level battery-life rule, not something any app can override.
  The in-widget refresh button (wired up in InventoryWidgetProvider)
  is there specifically so tapping it can get a fresher read on demand
  without waiting on that 30-minute floor.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="250dp"
    android:minHeight="110dp"
    android:targetCellWidth="4"
    android:targetCellHeight="2"
    android:updatePeriodMillis="1800000"
    android:initialLayout="@layout/inventory_widget"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:previewImage="@drawable/widget_background"
    android:description="@string/widget_description" />
